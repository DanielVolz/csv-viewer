# Local environment defaults (sanitized)
# Copy of .env.dev with obfuscated secrets and a neutral data path.

# OpenSearch credentials (placeholders)
OPENSEARCH_INITIAL_ADMIN_PASSWORD=changeme
OPENSEARCH_PASSWORD=changeme

# Data path (relative to repo; maps to /app/data in containers)
CSV_FILES_DIR=./data

# Port configurations (dev)
FRONTEND_DEV_PORT=5000
BACKEND_DEV_PORT=8000
BACKEND_PORT=8000

# Service ports
REDIS_PORT=6379
OPENSEARCH_PORT=9200
OPENSEARCH_TRANSPORT_PORT=9600
OPENSEARCH_DASHBOARDS_PORT=5601

# Optional proxy settings (uncomment if needed)
# HTTP_PROXY=
# HTTPS_PROXY=
# NO_PROXY=
